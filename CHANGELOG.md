# 更新日志 (Changelog)

## v1.0.0 — 2026-08-12

### 修复：鸿蒙 ↔ 鸿蒙设备互联

- **发现**：将 UDP 发现的"自过滤"逻辑由 deviceId 判定改为**源 IP 判定**。两台鸿蒙设备（deviceId 相同、IP 不同）现在可以互相发现；仅丢弃本机自身广播的回环包。
- **连接**：新增 **poke 识别**——收到对端连接请求（TCP 发送身份包后立即关闭）时，被连接端**静默回连**，不再误弹"连接已断开，可再次点连接"；连接建立后进入正常配对流程。
- 两台鸿蒙设备可完成：发现 → 连接 → TLS 加密 → v8 安全身份交换 → 配对 → 剪贴板/电量等同步。

### 修复：与安卓 / Windows / macOS 桌面端互连

- 恢复本机 `deviceId` 与内置 TLS 证书 CN 一致（`a1b2c3d4e5f6789012345678abcdef01`）。桌面/安卓 KDE Connect 端校验"身份包 deviceId 与对端证书 CN 一致"，此前不一致会导致对端握手成功后立即断连。
- 说明：HarmonyOS 公共 SDK 不支持运行时生成 X.509 自签证书，故 deviceId 保持与内置证书 CN 一致；鸿蒙设备间通过 IP 自过滤保证互相发现。

### 构建

- `build-profile.json5`：`entry` 模块 target 的 `applyToProducts` 补充 `"release"`，支持构建 release 签名产物（整包 `.app` / HAP）。

### 验证

- ArkTS 静态检查、Debug / Release 构建通过。
- 真机验证：UDP 发现/接收/解析正常、IP 自过滤正常、与桌面端及安卓端连接恢复、鸿蒙↔鸿蒙（模拟对端）发现/连接/TLS/安全身份/业务数据收发正常。
